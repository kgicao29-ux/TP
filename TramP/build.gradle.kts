// CloudStream Trạm Phim provider
version = 1

cloudstream {
    description = "Trạm Phim (tramphim3.org) — phim vietsub, thuyết minh, phim bộ, phim chiếu rạp. Servers: VSmov (direct HLS + subtitles), KKPhim/Nguonc/ViCDN backups."
    authors = listOf("kgicao29-ux")
    status = 1
    tvTypes = listOf("Movie", "TvSeries", "Anime", "AnimeMovie")
    language = "vi"
    requiresResources = false
    iconUrl = "https://tramphim3.org/favicon.ico"
}
